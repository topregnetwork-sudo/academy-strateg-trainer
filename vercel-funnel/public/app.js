const form=document.querySelector('#candidateForm'),notice=document.querySelector('#notice'),button=form.querySelector('button');
const task=document.createElement('fieldset');task.className='qualification';task.innerHTML='<legend>Тестовое задание <b>*</b></legend><p>Кандидат пропустил два обязательных задания и не отвечает третий день. Какими будут ваши первые действия как тренера?</p><textarea name="testAnswer" required minlength="40" placeholder="Опишите короткий и конкретный план действий"></textarea>';
form.querySelector('.form-divider').before(task);
form.addEventListener('submit',async event=>{event.preventDefault();if(!form.reportValidity())return;button.disabled=true;button.textContent='Сохраняем анкету…';notice.textContent='';const data=Object.fromEntries(new FormData(form));data.sourceId=new URLSearchParams(location.search).get('source_id')||'direct';try{const response=await fetch('/api/application',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const result=await response.json();if(!response.ok)throw new Error(result.error||'Не удалось сохранить анкету');location.href=result.telegramUrl}catch(error){notice.textContent=error.message||'Не удалось сохранить анкету. Попробуйте ещё раз или напишите @HRAcademyStrateg';button.disabled=false;button.textContent='Сохранить анкету и продолжить в Telegram'}});
