# CANDIDATE-DECLINE-041

Baseline342dacb, rollback/candidate-decline-before-041. User explicitly requests cancellation side effects: manual cancelled or bot decline releases future reservations and sends staff notice + replacement current summary, updates available slot buttons. Recover Victoria Kapustinskaya's existing manual decline after exact live identification. Staff destination same as original appointment brief (legacy review: coordination30, primary: configured primary topic). Never send candidate's personal data to public candidate group. No message to Victoria herself, no removal from group, no other candidate status/content changes.

Preserve all tests/answers, old messages, past reservations, manual outcomes, 039 click window, 040 stage projections and all-city reminders. Archive cancelled future booking rows transactionally before removing from active capacity tables (legacy unconditional unique key means status-only cancellation would not free physical slot). Event ledger + exact task, no periodic scan; retries idempotent. Update panel save feedback for this exception to silent status changes. Test capacity reuse, replay, rollback on error, native+legacy, no unrelated record changes, candidate decline excludes old reminders.
# Live verification

Implementation4013855 preview success then production.26 regression tests passed. Victoria Kapustinskaya id108 matched uniquely; user-set cancelled still had consenttrue and Sep1 13:15 booked. Event9ff4e236-5b2c-43ce-98ae-4a3201b2d3fb archived and cancelled one future reservation. Consentfalse; no personal send or group removal. Staff notice605 and replacement summary606 accepted Telegram in chat-1004397133749/topic30; notified_at2026-08-30T20:02:42.398Z/errornull. Five bookings remain;13:15 count0 and generated invitation keyboard includes offline_minsk_20260901_1315. Eligible unbooked invitations refreshed with strict error reporting. Old brief messages remain historical, not edited. No live new candidate booked the freed slot during testing; reuse verified in local SQL with original unique constraints.

Rollback: revert implementation and cleanup together, never publish intermediate maintenance route. Leave explicit user refusal cancelled: code rollback must NOT automatically reinstate Victoria, restore consent, or send another invitation. Full reservation snapshot remains in candidate_decline_events. If user explicitly revokes refusal, restore/create a reservation only after checking vacancy; never displace a new occupant. Cancel pending candidate_declined tasks only as part of rollback of this dispatcher, retaining ledger. Existing candidate data, messages, answers and groups untouched. Main Vercel published; Cloudflare static source mirrors synchronized, Worker static deployment not performed.
