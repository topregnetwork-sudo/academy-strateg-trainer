# FUNNEL-EVIDENCE-038

Baseline ed8d579; rollback branch rollback/funnel-evidence-before-038 (pushed).

Scope: show every saved Garcia answer without interpreting an unchecked optional box as refusal; record the first authenticated primary-Zoom callback, gate new keyword access on that event, calculate elapsed time from it, schedule missing September1 Minsk reminders/briefs and audit primary timer activation.

Preserve: existing advanced candidates and all answers, manual productivity decisions, source/city filters, messages/reply/broadcast layout, existing bookings/capacity/deadlines, Nadezhda45, primary Zoom URL versus separate Minsk review URL. No new Chelyabinsk review campaign. No fake historical clicks. No immediate bulk reminders.

Verify: first/repeated/foreign callback, keyword blocked before click, legacy access, optional checkbox states, elapsed-time unknown/backwards handling, idempotent scoped timers, both city rules, existing regression suite. Preview before production; record live migration and verification limits. Rollback code only; retain factual clicks/messages/bookings; cancel only pending new review tasks if rolling back timer support.
